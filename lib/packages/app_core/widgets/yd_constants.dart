class YDConstant {
  /// Usually uses for [TextFormField] to avoid calling API on each user interaction.
  static const typingDelay = Duration(milliseconds: 500);
  static const int maxPhoneNumberLength = 10;
  static const String ydCustomId = "yd-custom-id";
}
